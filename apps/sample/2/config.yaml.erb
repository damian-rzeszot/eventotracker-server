---
entrants_list:
  url: <%= url("/#{@app_name}/#{@event_id}/entrants.json") %>

entrants_details:
  url: <%= url("/example") %>

tracking:
  update_freq: 180
  data: <%= url("/example") %>
  paths:
    - name: red path
      url: <%= url("/example") %>

theme:
  accent:
    dark: "#007AFF"
    light: "#007AFF"
